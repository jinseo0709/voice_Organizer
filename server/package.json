{
  "name": "voice-organizer-server",
  "version": "1.0.0",
  "description": "GCP Speech-to-Text API 중계 서버",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required'",
    "deploy": "gcloud run deploy voice-organizer-server --source . --region asia-northeast3 --allow-unauthenticated"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "@google-cloud/speech": "^7.2.1",
    "@google-cloud/storage": "^7.7.0",
    "firebase-admin": "^12.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}