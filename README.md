# 🎙️ Voice Organizer V2

음성을 녹음하고 AI로 분석하여 메모로 저장하는 웹 애플리케이션입니다.

---

## 🌐 배포 URL

| 서비스 | URL |
|--------|-----|
| **웹 애플리케이션** | https://voice-organizer-480015.web.app |
| **API 서버** | https://voice-organizer-server-565683939264.asia-northeast1.run.app |

---

## 📱 사용 방법

### 1. 웹사이트 접속
브라우저에서 **https://voice-organizer-480015.web.app** 에 접속합니다.

### 2. 로그인
다음 중 하나의 방법으로 로그인할 수 있습니다:

| 로그인 방식 | 설명 |
|------------|------|
| **Google 로그인** | Google 계정으로 간편 로그인 |
| **이메일 로그인** | 이메일과 비밀번호로 로그인/회원가입 |
| **익명 로그인** | 계정 없이 바로 사용 |
| **데모 모드** | 로그인 없이 기능 체험 |

### 3. 음성 녹음
1. 메인 화면에서 **🎤 마이크 버튼**을 클릭합니다
2. 브라우저에서 마이크 권한을 요청하면 **허용**을 클릭합니다
3. 말하기 시작하면 녹음이 진행됩니다
4. 녹음을 마치려면 **⏹️ 정지 버튼**을 클릭합니다

### 4. AI 분석
녹음이 완료되면 자동으로 처리됩니다:

1. **음성 → 텍스트 변환** (Google Speech-to-Text API)
2. **AI 텍스트 분석** (Google Gemini AI)
   - 📝 내용 요약
   - 📂 카테고리 분류
   - 🏷️ 키워드 추출
   - 😊 감정 분석

### 5. 메모 저장 및 관리
- 분석 결과를 확인하고 **저장** 버튼을 클릭합니다
- 저장된 메모는 **메모 목록**에서 확인할 수 있습니다
- 메모 검색, 수정, 삭제가 가능합니다

---

## 🏗️ 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                      사용자 브라우저                          │
│                  (PWA 지원, 오프라인 캐시)                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Firebase Hosting                          │
│              (Next.js 정적 사이트 호스팅)                      │
│          https://voice-organizer-480015.web.app              │
└─────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
┌──────────────────┐ ┌──────────────┐ ┌──────────────────┐
│  Firebase Auth   │ │   Firestore  │ │    Cloud Run     │
│  (Google/Email/  │ │   Database   │ │    API Server    │
│   Anonymous)     │ │  (메모 저장)  │ │  (음성/AI 처리)   │
└──────────────────┘ └──────────────┘ └──────────────────┘
                                              │
                              ┌───────────────┼───────────────┐
                              ▼               ▼               ▼
                    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
                    │     GCP      │ │    Gemini    │ │    Cloud     │
                    │  Speech-to-  │ │    AI API    │ │   Storage    │
                    │    Text      │ │              │ │              │
                    └──────────────┘ └──────────────┘ └──────────────┘
```

---

## 🛠️ 기술 스택

### Frontend
| 기술 | 버전 | 용도 |
|------|------|------|
| Next.js | 15.x | React 프레임워크 (정적 export) |
| TypeScript | 5.x | 타입 안전성 |
| Tailwind CSS | 3.x | 스타일링 |
| PWA | - | 프로그레시브 웹 앱 지원 |

### Backend
| 기술 | 버전 | 용도 |
|------|------|------|
| Node.js | 18+ | 런타임 |
| Express | 4.x | API 서버 |
| Cloud Run | - | 서버리스 컨테이너 |

### AI & Cloud Services
| 서비스 | 용도 |
|--------|------|
| Google Speech-to-Text | 음성 인식 |
| Google Gemini AI | 텍스트 분석 |
| Firebase Authentication | 사용자 인증 |
| Cloud Firestore | NoSQL 데이터베이스 |
| Firebase Hosting | 웹 호스팅 |

### Build Tools
| 도구 | 용도 |
|------|------|
| Turborepo | 모노레포 빌드 시스템 |
| pnpm | 패키지 매니저 |

---

## 📁 프로젝트 구조

```
voice_Organizer/
├── apps/
│   └── web/                    # Next.js 웹 애플리케이션
│       ├── src/
│       │   ├── app/            # App Router 페이지
│       │   ├── components/     # React 컴포넌트
│       │   │   ├── audio/      # 음성 녹음/처리
│       │   │   ├── auth/       # 인증 관련
│       │   │   └── memo/       # 메모 관련
│       │   ├── contexts/       # React Context
│       │   ├── hooks/          # Custom Hooks
│       │   ├── lib/            # 유틸리티
│       │   └── types/          # TypeScript 타입
│       ├── public/             # 정적 파일
│       └── next.config.ts      # Next.js 설정
├── server/                     # Cloud Run API 서버
│   ├── server.js               # Express 서버
│   ├── package.json            # 서버 의존성
│   └── Dockerfile              # 컨테이너 설정
├── firebase.json               # Firebase 설정
├── firestore.rules             # Firestore 보안 규칙
├── storage.rules               # Storage 보안 규칙
└── turbo.json                  # Turborepo 설정
```

---

## 🚀 로컬 개발 환경 설정

### 사전 요구사항
- Node.js 18+
- pnpm
- Firebase CLI (`npm install -g firebase-tools`)
- Google Cloud CLI (`gcloud`)

### 1. 저장소 클론
```bash
git clone <repository-url>
cd voice_Organizer
```

### 2. 의존성 설치
```bash
pnpm install
```

### 3. 환경 변수 설정

**Frontend (`apps/web/.env.local`)**:
```env
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
NEXT_PUBLIC_API_URL=http://localhost:8080
```

**Server (`server/.env`)**:
```env
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
GEMINI_API_KEY=your-gemini-api-key
PORT=8080
```

### 4. 개발 서버 실행
```bash
# 프론트엔드 (루트 디렉토리에서)
npm run dev

# 또는 개별 실행
cd apps/web && pnpm dev

# 백엔드 (별도 터미널)
cd server && pnpm dev
```

### 5. 브라우저에서 확인
http://localhost:3000 접속

---

## 📦 배포

### Frontend (Firebase Hosting)
```bash
# 빌드
npx turbo run build

# 배포
firebase deploy --only hosting
```

### Backend (Cloud Run)
```bash
cd server

# 배포
gcloud run deploy voice-organizer-server \
  --source . \
  --region asia-northeast1 \
  --allow-unauthenticated
```

---

## 🔧 API 엔드포인트

### `POST /api/speech-to-text`
음성 파일을 텍스트로 변환

**Request**:
```
Content-Type: multipart/form-data
Body: audio (파일)
```

**Response**:
```json
{
  "success": true,
  "text": "변환된 텍스트",
  "confidence": 0.95
}
```

### `POST /api/gemini-analysis`
Gemini AI를 사용한 텍스트 분석

**Request**:
```json
{
  "text": "분석할 텍스트"
}
```

**Response**:
```json
{
  "success": true,
  "analysis": {
    "summary": "요약",
    "category": "카테고리",
    "keywords": ["키워드1", "키워드2"],
    "sentiment": "positive"
  }
}
```

### `POST /api/text-analysis`
간단한 텍스트 분석 (Gemini 없이)

**Request**:
```json
{
  "text": "분석할 텍스트"
}
```

**Response**:
```json
{
  "success": true,
  "analysis": {
    "wordCount": 10,
    "charCount": 50,
    "summary": "간단 요약"
  }
}
```

### `GET /health`
서버 상태 확인

**Response**:
```json
{
  "status": "ok",
  "timestamp": "2025-12-05T..."
}
```

---

## 🔐 보안

- **Firebase Authentication**: 사용자 인증 (Google, Email, Anonymous)
- **Firestore 보안 규칙**: 데이터 접근 제어
- **CORS 설정**: 허용된 도메인만 API 접근 가능
- **환경 변수**: 민감한 정보 관리

---

## 🔄 개발 로그

### 2025-12-05
- ✅ Frontend를 정적 export로 전환
- ✅ Cloud Run API 서버와 직접 연결
- ✅ Firebase Hosting 배포 완료
- ✅ CORS 설정 업데이트

### 2025-12-04
- ✅ Google AI Studio API 통합 완료
- ✅ Gemini Pro 모델을 통한 텍스트 분석 구현
- ✅ 자동 카테고리 분류 시스템 구축
- ✅ Cloud Run 프로덕션 서버 배포
- ✅ 프로젝트 구조 정리 및 최적화

---

## 🐛 문제 해결

### 일반적인 이슈

| 문제 | 해결 방법 |
|------|----------|
| 서버 연결 오류 | Cloud Run 서비스 상태 확인 |
| 음성 인식 실패 | 오디오 포맷 및 마이크 권한 확인 |
| API 키 오류 | 환경 변수 설정 확인 |
| 빌드 에러 | `pnpm install` 재실행 |

### 서버 상태 확인
```bash
# Cloud Run 서버 헬스체크
curl https://voice-organizer-server-565683939264.asia-northeast1.run.app/health
```

---

## 📄 라이선스

MIT License

---

## 🤝 기여

이슈와 풀 리퀘스트를 환영합니다!

---

**🎙️ Voice Organizer** - AI 기반 스마트 음성 텍스트 관리 시스템
